@model IEnumerable<QLTHPT.Models.Tiethoc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThTen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ThTen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ThMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
