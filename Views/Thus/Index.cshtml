@model IEnumerable<QLTHPT.Models.Thu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThuMa)
                @Html.DisplayNameFor(model => model.ThuTen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
              <td>
                @Html.DisplayFor(modelItem => item.ThuMa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThuTen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ThuMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThuMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThuMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
